{
    "what-it-does": "Checks for duplicate union members.",
    "why-is-this-bad": "Duplicate union members are redundant and should be removed.",
    "example": "foo: str | str\n\n\n\nUse instead:\n\n\nfoo: str",
    "fix-safety": "This rule's fix is marked as safe unless the union contains comments.\n\n\nFor nested union, the fix will flatten type expressions into a single\ntop-level union.",
    "example-split": {
        "example-before": "foo: str | str\n\n",
        "example-after": "\nfoo: str"
    }
}