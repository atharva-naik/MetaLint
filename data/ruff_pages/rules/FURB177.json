{
    "what-it-does": "Checks for current-directory lookups using Path().resolve().",
    "why-is-this-bad": "When looking up the current directory, prefer Path.cwd() over\nPath().resolve(), as Path.cwd() is more explicit in its intent.",
    "example": "cwd = Path().resolve()\n\n\n\nUse instead:\n\n\ncwd = Path.cwd()",
    "example-split": {
        "example-before": "cwd = Path().resolve()\n\n",
        "example-after": "\ncwd = Path.cwd()"
    }
}