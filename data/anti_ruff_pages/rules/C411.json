{
    "what-it-does": "Checks for unnecessary list() calls around list comprehensions.",
    "why-is-this-bad": "It is redundant to use a list() call around a list comprehension.",
    "fix-safety": "This rule's fix is marked as unsafe, as it may occasionally drop comments\nwhen rewriting the call. In most cases, though, comments will be preserved."
}