{
    "what-it-does": "Checks for if expressions that check against a negated condition.",
    "why-is-this-bad": "if expressions that check against a negated condition are more difficult\nto read than if expressions that check against the condition directly.",
    "example": "b if not a else a\n\n\n\nUse instead:\n\n\na if a else b",
    "example-split": {
        "example-before": "b if not a else a\n\n",
        "example-after": "\na if a else b"
    }
}