{
    "what-it-does": "Checks for mismatched argument types in \"old-style\" format strings.",
    "why-is-this-bad": "The format string is not checked at compile time, so it is easy to\nintroduce bugs by mistyping the format string.",
    "example": "print(\"%d\" % \"1\")\n\n\n\nUse instead:\n\n\nprint(\"%d\" % 1)",
    "example-split": {
        "example-before": "print(\"%d\" % \"1\")\n\n",
        "example-after": "\nprint(\"%d\" % 1)"
    }
}