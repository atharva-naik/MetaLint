{
    "what-it-does": "Checks for ternary if expressions that can be replaced with the or\noperator.",
    "why-is-this-bad": "Ternary if expressions are more verbose than or expressions while\nproviding the same functionality.",
    "example": "z = x if x else y\n\n\n\nUse instead:\n\n\nz = x or y",
    "fix-safety": "This rule's fix is marked as unsafe in the event that the body of the\nif expression contains side effects.\n\n\nFor example, foo will be called twice in foo() if foo() else bar()\n(assuming foo() returns a truthy value), but only once in\nfoo() or bar().",
    "example-split": {
        "example-before": "z = x if x else y\n\n",
        "example-after": "\nz = x or y"
    }
}