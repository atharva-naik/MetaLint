{
    "what-it-does": "Checks for uses of undefined names.",
    "why-is-this-bad": "An undefined name is likely to raise NameError at runtime.",
    "example": "def double():\n    return n * 2  # raises `NameError` if `n` is undefined when `double` is called\n\n\n\nUse instead:\n\n\ndef double(n):\n    return n * 2",
    "example-split": {
        "example-before": "def double():\n    return n * 2  # raises `NameError` if `n` is undefined when `double` is called\n\n",
        "example-after": "\ndef double(n):\n    return n * 2"
    }
}