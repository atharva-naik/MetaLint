{
    "what-it-does": "Checks for dict.get(key, falsy_value) calls in boolean test positions.",
    "why-is-this-bad": "The default fallback None is already falsy.",
    "example": "if dict.get(key, False):\n    ...\n\n\n\nUse instead:\n\n\nif dict.get(key):\n    ...",
    "fix-safety": "This rule's fix is marked as safe, unless the dict.get() call contains comments between arguments.",
    "example-split": {
        "example-before": "if dict.get(key, False):\n    ...\n\n",
        "example-after": "\nif dict.get(key):\n    ..."
    }
}