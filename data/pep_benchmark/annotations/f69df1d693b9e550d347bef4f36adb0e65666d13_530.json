{
    "blob_id": "f69df1d693b9e550d347bef4f36adb0e65666d13",
    "violations": [
        {
            "start_lineno": 19,
            "end_lineno": 22,
            "pep": "530",
            "line": "tasks = []\n    for symbol in symbols:\n        tasks.append(asyncio.create_task(session.get(url.format(symbol, api_key), ssl=False)))\n    return tasks",
            "span": "for symbol in symbols",
            "fix": [
                {
                    "before": "tasks = []\n    for symbol in symbols:\n        tasks.append(asyncio.create_task(session.get(url.format(symbol, api_key), ssl=False)))\n    return tasks",
                    "after": "return [\n        asyncio.create_task(session.get(url.format(symbol, api_key), ssl=False))\n        for symbol in symbols\n    ]"
                }
               
            ]
        },
        {
            "start_lineno": 29,
            "end_lineno": 30,
            "pep": "530",
            "line": "for response in responses:\n            results.append(await response.json())",
            "span": "for response in responses",
            "fix": [
                {
                    "before": "for response in responses:\n            results.append(await response.json())",
                    "after": "        results.extend([await response.json() for response in responses])"
                }
               
            ]
        }

    ]
}