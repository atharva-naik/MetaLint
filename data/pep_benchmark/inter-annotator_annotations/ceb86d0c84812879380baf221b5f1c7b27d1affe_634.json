{
    "blob_id": "ceb86d0c84812879380baf221b5f1c7b27d1affe",
    "violations": [
        {
            "start_lineno": 23,
            "end_lineno": 54,
            "pep": "634",
            "line": "if self.max_score == 0 and score > 0: position = util.from_relative(det_rel, frame_scaled.shape) \n position = util.fit_rect(position, frame_scaled.shape) \n self.tracker.start_track(frame_scaled, util.to_dlib(position)) \n self.max_score = score \n if self.max_score > 0: \n self.tracker.update(frame_scaled) \n position = util.fit_rect(util.from_dlib(self.tracker.get_position()), frame_scaled.shape) \n pos_rel = util.to_relative(position, frame_scaled.shape) \n if score <= 0: \n self.empty_frames += 1 \n if self.empty_frames >= self.options[consts.empty_frames]: \n self.max_score = 0 \n self.empty_frames = 0 \n else: \n self.empty_frames = 0 \n if util.are_different_locations(pos_rel, det_rel): \n self.wrong_frames += 1 \n if self.wrong_frames == 5: \n self.wrong_frames = 0 \n self.wrong_frames = 0 \n position = util.from_relative(det_rel, frame_scaled.shape) \n position = util.fit_rect(position, frame_scaled.shape) \n self.tracker.start_track(frame_scaled, util.to_dlib(position)) \n self.max_score = score \n else: \n self.wrong_frames = 0 \n rect = util.from_relative(pos_rel, frame.shape) \n hand_rect = util.to_square(rect, True) \n return hand_rect \n else: \n return None",
            "span": "if self.max_score == 0 and score > 0",
            "fix": [
                {
                    "before": "if self.max_score == 0 and score > 0: position = util.from_relative(det_rel, frame_scaled.shape) \n position = util.fit_rect(position, frame_scaled.shape) \n self.tracker.start_track(frame_scaled, util.to_dlib(position)) \n self.max_score = score \n if self.max_score > 0: \n self.tracker.update(frame_scaled) \n position = util.fit_rect(util.from_dlib(self.tracker.get_position()), frame_scaled.shape) \n pos_rel = util.to_relative(position, frame_scaled.shape) \n if score <= 0: \n self.empty_frames += 1 \n if self.empty_frames >= self.options[consts.empty_frames]: \n self.max_score = 0 \n self.empty_frames = 0 \n else: \n self.empty_frames = 0 \n if util.are_different_locations(pos_rel, det_rel): \n self.wrong_frames += 1 \n if self.wrong_frames == 5: \n self.wrong_frames = 0 \n self.wrong_frames = 0 \n position = util.from_relative(det_rel, frame_scaled.shape) \n position = util.fit_rect(position, frame_scaled.shape) \n self.tracker.start_track(frame_scaled, util.to_dlib(position)) \n self.max_score = score \n else: \n self.wrong_frames = 0 \n rect = util.from_relative(pos_rel, frame.shape) \n hand_rect = util.to_square(rect, True) \n return hand_rect \n else: \n return None",
                    "after": "match self.max_score, score: \n case 0, score if score > 0: \n position = util.from_relative(det_rel, frame_scaled.shape) \n position = util.fit_rect(position, frame_scaled.shape) \n self.tracker.start_track(frame_scaled, util.to_dlib(position)) \n self.max_score = score \n case _, _ if self.max_score > 0: \n self.tracker.update(frame_scaled) \n position = util.fit_rect(util.from_dlib(self.tracker.get_position()), frame_scaled.shape) \n pos_rel = util.to_relative(position, frame_scaled.shape) \n match score: \n case score if score <= 0: \n self.empty_frames += 1 \n if self.empty_frames >= self.options[consts.empty_frames]: \n self.max_score = 0 \n self.empty_frames = 0 \n case _: \n self.empty_frames = 0 \n match util.are_different_locations(pos_rel, det_rel): \n case True: \n self.wrong_frames += 1 \n if self.wrong_frames == 5: \n self.wrong_frames = 0 \n position = util.from_relative(det_rel, frame_scaled.shape) \n position = util.fit_rect(position, frame_scaled.shape) \n self.tracker.start_track(frame_scaled, util.to_dlib(position)) \n self.max_score = score \n case False: \n self.wrong_frames = 0 \n rect = util.from_relative(pos_rel, frame.shape) \n hand_rect = util.to_square(rect, True) \n return hand_rect \n case _: \n return None"
                }
            ]
        }
    ]
}
