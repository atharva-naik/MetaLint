{
    "what-it-does": "Checks for int conversions of values that are already integers.",
    "why-is-this-bad": "Such a conversion is unnecessary.",
    "example": "int(len([]))\nint(round(foo, None))\n\n\n\nUse instead:\n\n\nlen([])\nround(foo)",
    "fix-safety": "The fix for round, math.ceil, math.floor, and math.truncate is unsafe\nbecause removing the int conversion can change the semantics for values\noverriding the __round__, __ceil__, __floor__, or __trunc__ dunder methods\nsuch that they don't return an integer.",
    "example-split": {
        "example-before": "int(len([]))\nint(round(foo, None))\n\n",
        "example-after": "\nlen([])\nround(foo)"
    }
}