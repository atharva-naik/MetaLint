{
    "what-it-does": "Checks for set() calls that take unnecessary list or tuple literals\nas arguments.",
    "why-is-this-bad": "It's unnecessary to use a list or tuple literal within a call to set().\nInstead, the expression can be rewritten as a set literal.",
    "fix-safety": "This rule's fix is marked as unsafe, as it may occasionally drop comments\nwhen rewriting the call. In most cases, though, comments will be preserved."
}