{
    "what-it-does": "Check for shallow os.environ copies.",
    "why-is-this-bad": "os.environ is not a dict object, but rather, a proxy object. As such, mutating a shallow\ncopy of os.environ will also mutate the original object.\n\n\nSee: #15373 for more information.",
    "example": "import copy\nimport os\n\nenv = copy.copy(os.environ)\n\n\n\nUse instead:\n\n\nimport os\n\nenv = os.environ.copy()",
    "example-split": {
        "example-before": "import copy\nimport os\n\nenv = copy.copy(os.environ)\n\n",
        "example-after": "\nimport os"
    }
}