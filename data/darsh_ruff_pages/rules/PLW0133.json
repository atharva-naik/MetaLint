{
    "what-it-does": "Checks for an exception that is not raised.",
    "why-is-this-bad": "It's unnecessary to create an exception without raising it. For example,\nValueError(\"...\") on its own will have no effect (unlike\nraise ValueError(\"...\")) and is likely a mistake.",
    "example": "ValueError(\"...\")\n\n\n\nUse instead:\n\n\nraise ValueError(\"...\")",
    "fix-safety": "This rule's fix is marked as unsafe, as converting a useless exception\nstatement to a raise statement will change the program's behavior.",
    "example-split": {
        "example-before": "ValueError(\"...\")\n\n",
        "example-after": "\nraise ValueError(\"...\")"
    }
}