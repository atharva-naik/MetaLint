{
    "what-it-does": "Checks for str.join calls that can be replaced with f-strings.",
    "why-is-this-bad": "f-strings are more readable and generally preferred over str.join calls.",
    "example": "\" \".join((foo, bar))\n\n\n\nUse instead:\n\n\nf\"{foo} {bar}\"",
    "example-split": {
        "example-before": "\" \".join((foo, bar))\n\n",
        "example-after": "\nf\"{foo} {bar}\""
    }
}