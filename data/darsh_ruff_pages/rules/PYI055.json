{
    "what-it-does": "Checks for the presence of multiple types in a union.",
    "why-is-this-bad": "type[T | S] has identical semantics to type[T] | type[S] in a type\nannotation, but is cleaner and more concise.",
    "example": "field: type[int] | type[float] | str\n\n\n\nUse instead:\n\n\nfield: type[int | float] | str",
    "fix-safety": "This rule's fix is marked as safe, unless the type annotation contains comments.\n\n\nNote that while the fix may flatten nested unions into a single top-level union,\nthe semantics of the annotation will remain unchanged.",
    "example-split": {
        "example-before": "field: type[int] | type[float] | str\n\n",
        "example-after": "\nfield: type[int | float] | str"
    }
}